export const getProfile = async ({own}) => {
    // TODO connect to backend when PR will be merged
    own
    return {
        first_name: "first_name",
        last_name: "last_name",
        ava_url: "",
        phone_number: "phone_number",
        email: "email",
    };
};

export const editProfile = async (payload) => {
    // TODO connect to backend when PR will be merged
    return payload;
}
